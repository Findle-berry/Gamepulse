# GamePulse MVP

Double-click `start-gamepulse.cmd`, keep that window open, then open `http://localhost:3000`.

Alternatively, run `npm start` from this folder.

The browser talks only to the local backend. The backend collects live Steam search, store metadata, recent English Steam reviews, and official Steam news. The score is the percentage of positive recent reviews—not an invented AI rating.
